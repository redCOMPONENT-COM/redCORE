class_name: ApiTester
modules:
    enabled:
      - ApiHelper
      - Asserts
      - REST:
          depends: PhpBrowser
          url: http://localhost/tests/joomla/
      - SOAP:
          depends: PhpBrowser
          endpoint: api_url
          schema: wsdl
