language: php

php:
  - 5.3

before_script:
  - pyrus install -f pear/PHP_CodeSniffer
  - pyrus channel-discover pear.phing.info
  - pyrus install -f phing/phing
  - phpenv rehash

script:
  - phing travis-phpcs
