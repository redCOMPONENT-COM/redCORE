language: php

php:
  - 5.3
  - 5.4

before_script:
  - pyrus install -f pear/PHP_CodeSniffer
  - phpenv rehash
  - git clone http://github.com/joomla/coding-standards.git $(pear config-get php_dir)/PHP/CodeSniffer/Standards/Joomla

script:
  - phpcs --standard=Joomla libraries
