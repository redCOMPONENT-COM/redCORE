language: php

php:
  - 5.3

before_script:
  - pyrus install -f pear/PHP_CodeSniffer
  - pear channel-discover pear.phing.info
  - pear install phing/phing
  - phpenv rehash

script:
  - phing travis-phpcs
